---
# vars file for last_config
moodle_host: "{{ vault_moodle_host }}"
moodle_db: "{{ vault_moodle_db }}"
moodle_user: "{{ vault_moodle_user }}"
moodle_user_password: "{{ vault_moodle_user_password }}"
